# Enchant Master — Paper 26.1.2

Paper server plugin. Optional Fabric client: `ports/fabric-client-26.1.2`.

**1.16.5 Paper is deferred** (Forge-only special line).

## Build
```bash
cd ports/paper-26.1.2 && ./gradlew build
```
