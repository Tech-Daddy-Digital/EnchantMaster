# Enchant Master — Paper 1.20.1

Paper server plugin. Optional Fabric client: `ports/fabric-client-1.20.1`.

**1.16.5 Paper is deferred** (Forge-only special line).

## Build
```bash
cd ports/paper-1.20.1 && ./gradlew build
```
